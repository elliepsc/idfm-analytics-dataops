[isort]
profile = black
known_first_party = utils

[flake8]
max-line-length = 120
extend-ignore = E203
per-file-ignores =
    # Tests use sys.path.insert before imports (E402) and have unused imports from fixtures (F401)
    tests/unit/*.py: E402, F401
